//! Venue trait — abstracts exchanges (Hyperliquid, UniV3, CEX) behind one surface.
//!
//! Phase 0 placeholder. Real trait lands in #2.

pub fn it_works() -> bool {
    true
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn placeholder() {
        assert!(it_works());
    }
}
