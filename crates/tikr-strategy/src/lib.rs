//! Strategy trait — quoting logic that turns market state into orders.
//!
//! Phase 0 placeholder. Real trait lands in #4.

pub fn it_works() -> bool {
    true
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn placeholder() {
        assert!(it_works());
    }
}
